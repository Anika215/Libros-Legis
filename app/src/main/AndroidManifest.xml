<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.divya.libroslegis">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_libros"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_libros_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Maternity" />
        <activity android:name=".Harrass" />
        <activity android:name=".Equal" />
        <activity android:name=".Factories" />
        <activity android:name=".Violence" />
        <activity android:name=".Work" />
        <activity android:name=".fabChatbot"></activity>
    </application>

</manifest>